# Variáveis de Ambiente para Produção
# Copie este arquivo e renomeie para .env.production
# Preencha com os valores reais

# ===========================================
# BACKEND (Render) - Variáveis de Ambiente
# ===========================================

# Perfil do Spring Boot
SPRING_PROFILES_ACTIVE=prod

# Configurações do Banco PostgreSQL (Supabase)
# Substitua pelos valores reais do seu projeto Supabase
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DB_USERNAME=postgres
DB_PASSWORD=[SUA-SENHA-SUPABASE]

# Configurações JWT
# Gere um secret forte (ex: usando openssl rand -base64 32)
JWT_SECRET=[GERE-UM-SECRET-FORTE-AQUI]
JWT_EXPIRATION=86400000

# URL do Frontend (Vercel)
# Substitua pelo domínio real do seu projeto Vercel
FRONTEND_URL=https://[SEU-PROJETO].vercel.app

# Porta (Render define automaticamente)
PORT=8080

# ===========================================
# FRONTEND (Vercel) - Variáveis de Ambiente
# ===========================================

# URL da API Backend (Render)
# Substitua pelo domínio real do seu backend no Render
NEXT_PUBLIC_API_URL=https://[SEU-BACKEND].onrender.com

# ===========================================
# SUPABASE - Informações do Projeto
# ===========================================

# Estas informações você encontra no dashboard do Supabase:
# Settings > Database

# Host: db.[PROJECT-REF].supabase.co
# Database name: postgres
# Port: 5432
# User: postgres
# Password: [A senha que você definiu]

# Connection String completa:
# postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# ===========================================
# INSTRUÇÕES
# ===========================================

# 1. Crie conta no Supabase (supabase.com)
# 2. Crie novo projeto
# 3. Anote as credenciais do banco
# 4. Substitua [PROJECT-REF] e [PASSWORD] na DATABASE_URL
# 5. Gere um JWT_SECRET forte
# 6. Configure as variáveis no Render (backend)
# 7. Configure NEXT_PUBLIC_API_URL no Vercel (frontend)

# ===========================================
# COMANDOS ÚTEIS
# ===========================================

# Gerar JWT Secret:
# openssl rand -base64 32

# Testar conexão com banco:
# psql "postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"